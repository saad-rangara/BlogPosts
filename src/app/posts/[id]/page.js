// need to usw params to render data dynamically
//metadata

export default function IdPage() {
  // get my post my post from the db filtering by id (WHERE id = ${params.id})
  // need handle submit form to update post
  return (
    <>
      // need to use params to render data dynamically // need to display an
      individual post and relevant data // need display from with inputs that
      connected to the comments table columns in my db
    </>
  );
}
